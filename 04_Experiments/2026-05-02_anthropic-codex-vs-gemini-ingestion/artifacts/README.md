# Artifacts

This experiment preserves the Codex baseline under `codex-worktree/`.

Contents:

- `git-status.txt`: worktree status at snapshot time
- `tracked.diff`: tracked-file diff from the primary worktree
- copied versions of all new and modified Anthropic-related files

Use this directory as the fixed comparison target after Gemini finishes the clean rerun.
